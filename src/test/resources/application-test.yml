spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: false

jwt:
  secret: test-secret-key-que-tiene-al-menos-256-bits-para-hmac-sha256-ok
  expiration: 3600000  # 1 ho